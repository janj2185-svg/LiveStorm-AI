apiVersion: v1
kind: Service
metadata:
  name: sylora-api
  labels:
    app.kubernetes.io/name: sylora-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sylora-api
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
